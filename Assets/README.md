# GhiblyMail Assets

This directory stores source visual assets for the cozy office UI before they are bundled into the Swift package.

Current layout:

- `Office/`: main office backgrounds and sliced scene layers.
- `Agents/`: assistant sprites and animation frames.
- `Props/`: reusable prop overlays and interactive objects.
- `Effects/`: lightweight visual effects.
